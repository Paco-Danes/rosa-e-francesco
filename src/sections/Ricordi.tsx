// STUB — sostituito dalla sezione completa.
export interface SectionProps {
  onBack: () => void
}

export default function Ricordi({ onBack }: SectionProps) {
  return <button onClick={onBack}>← (stub ricordi)</button>
}
