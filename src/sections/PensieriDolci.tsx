// STUB — sostituito dalla sezione completa.
export interface SectionProps {
  onBack: () => void
}

export default function PensieriDolci({ onBack }: SectionProps) {
  return <button onClick={onBack}>← (stub pensieri)</button>
}
