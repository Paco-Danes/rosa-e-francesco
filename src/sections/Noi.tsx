// STUB — sostituito dalla sezione completa.
export interface SectionProps {
  onBack: () => void
}

export default function Noi({ onBack }: SectionProps) {
  return <button onClick={onBack}>← (stub noi)</button>
}
